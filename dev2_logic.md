# 개발2팀 데이터 수집 및 적재 프로세스 절차서

본 문서는 개발2팀이 담당하는 시장 전체 공시 및 시세 데이터 수집/적재 파이프라인과 이를 안전하게 실행하기 위한 사전 서버 점검 절차를 정의합니다.

---

## 1. 개발2팀 수집 프로세스 및 사전 점검 (Mandatory Process)

SQLite 데이터베이스의 동시성 오류 및 Database Lock 충돌을 예방하고 안전하게 데이터를 적재하기 위해, **데이터 수집 및 적재 작업을 실행하기 전 반드시 시스템 구동 상태를 사전 점검**해야 합니다.

### [1단계] Flask 서버 구동 상태 점검 (DB 락 방지 가드)
* **목적**: Flask 실시간 거래 서버(`trade.py`)가 구동 중인 상태에서 백그라운드 대량 수집 및 쓰기 작업이 중첩되면 SQLite 파일에 락이 걸려 파이프라인이 멈추거나 실패합니다.
* **점검 절차**:
  1. 수집 스크립트 실행 시, 자동으로 5000번 포트 및 `trade.py` 프로세스를 조회하여 서버가 켜져 있는지 감지합니다.
  2. 서버 구동이 감지될 경우, 콘솔 프롬프트를 통해 사용자에게 진행 여부를 확인(`y/N`)합니다.
  3. 작업을 취소하면 안전하게 중단되며, 계속 진행할 경우 DB 락 리스크를 감안하고 연산을 실행합니다. (또는 `--force-db` 옵션을 통해 질의 단계 없이 강제 구동할 수 있습니다.)

### [2단계] DART 공시 수집 파이프라인 실행
* **대상 모듈**: [collect_disclosures.py](file:///c:/Pythons/trade/collect_disclosures.py)
* **명령어**: `python collect_disclosures.py --days <수집기간>`
* **역할**: DART API를 통해 시장의 수시공시 데이터를 수집하여 `stock_disclosures` 테이블에 전량 적재합니다.

### [3단계] 시장 데이터 수집 파이프라인 실행
* **대상 모듈**: [data_collect.py](file:///c:/Pythons/trade/data_collect.py)
* **명령어**: `python data_collect.py --count <종목수> --market <시장구분>`
* **역할**: 네이버 금융 등에서 종목별 최신 시세, 부채비율, 거래 정보 등을 수집하여 엑셀 및 DB에 적재합니다.
