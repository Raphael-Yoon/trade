# [Pool 섹터별 정성 스코어링 프롬프트 지침]

이 문서는 앱이 아니라 **Claude Code 세션(이 프롬프트)**이 직접 수행하는 작업 지침입니다.
**이 정성 스코어링 단계는 시스템(앱) 기능으로 구현하지 않는다.** Gemini 등 AI API를 코드에서 직접 호출하는 방식은 쓰지 않으며, 사람이 요청할 때마다 이 문서를 지침 삼아 Claude Code 세션이 뉴스·공시를 검색하고 결과를 JSON으로 만들어 수동으로 반영한다.
Pool 구성(재무데이터 기반 섹터별 상위 10개 선정)은 `cowork/pool_collect.py`가 이미 순수 정량 로직으로 처리한다. 이 문서는 그 결과물 위에서, 뉴스·공시 등 정성적 정보를 반영해 섹터별 최종 추천 종목의 우선순위를 정하는 절차를 정의한다.

---

## 1. 입력 데이터

* 대상 종목군: `tr_stock_pool` 테이블(= `pool_collect.py` 실행 결과)에 담긴 **섹터별 상위 10개** 종목. 섹터 목록과 종목 구성은 Pool 재구성 시마다 바뀔 수 있으므로, 매 작업 시점의 최신 Pool 데이터를 기준으로 한다.
* 섹터 구성: 총 8개 섹터(`is_sector_leader=1`) + 기타(`is_sector_leader=0`, 통합 상위 20개). 8개 섹터는 `sector_category` 값으로 두 그룹으로 나뉜다 — **`scale`**(영업이익 합계 상위 4개, 종목수 10개 이상 대상)과 **`growth`**(영업이익증가율·매출증가율 중앙값 복합 상위 4개, 동일 조건). 두 그룹 모두 동일한 절차로 정성 스코어링한다.
* 참고 재무 지표: 이미 계산되어 있는 `pool_score`(섹터 내 백분위 기준 — 영업이익 규모 50% + ROE 15% + PBR 15% + 영업이익증가율 10% + 부채비율 10%)와 PBR/PER/ROE/부채비율/매출·영업이익·순이익 증가율 등 원본 재무제표 지표.

---

## 2. 핵심 분석 지침 (섹터별 피어 그룹 내 정성 스코어링)

> [!IMPORTANT]
> **섹터 내 상대평가 원칙**
> * 서로 다른 업종 종목을 통합해서 하나의 순위로 매기지 않는다. (예: 반도체 종목과 금융/지주사 종목을 동일선상에 비교 금지)
> * 각 섹터의 Pool 상위 10개 후보 내에서만 순위를 산정한다.
> * 섹터별 후보 종목에 대해 **최근 뉴스·DART 공시를 검색**하여 재료(공급계약, 유상증자, 소송, 영업정지, 실적 서프라이즈 등)를 확인하고, 재무 스코어(`pool_score`)와 결합해 섹터당 **상위 1~3개** 종목을 최종 추천으로 선정한다.

> [!WARNING]
> **재무 건전성 및 회계 리스크 필터링**
> * 최근 DART 공시에서 **회계감사의견이 '적정'이 아닌 종목(의견거절, 한정의견 등)은 추천에서 즉시 제외**한다.
> * 부채비율이 **150%를 초과**하거나 지속적인 당기순손실을 내는 종목은 추천 순위에서 강등하거나 제외한다.

---

## 3. 데이터 해석 시 유의사항

1. **누적 실적 해석**: 분기·반기보고서 실적은 당해 연도 누적치이므로, 전년 사업보고서(12개월)와 단순 비교해 실적 악화로 오판하지 말고 연환산(Annualizing)하여 비교한다.
2. **결측치 제외**: 값이 0이거나 'NaN'/'N/A'인 지표는 근거로 사용하지 않는다.
3. **가독성**: `reason`, `news_summary`, `one_liner` 등 텍스트 필드에 큰 숫자를 쓸 때는 '조'/'억' 단위로 표기한다. (예: `401.6조`, `788억`)

---

## 4. 출력 형식 (시스템 반영용 JSON)

분석 리포트(마크다운)가 아니라, `trade/results/sector_recommendations.json` 파일에 **레코드 배열**로 저장한다. 이 파일은 `/api/targets/migrate` 엔드포인트가 읽어 `tr_audit_recommendations` 테이블에 적재하고, `/api/pool`이 Pool 데이터와 병합해 화면에 표시한다.

각 레코드(선정된 종목 1개당 1건) 필드:

```json
{
  "code": "종목코드(6자리)",
  "name": "종목명",
  "sector": "섹터명 (Pool의 업종과 동일해야 매칭됨)",
  "current_price": 0,
  "target_price": 0,
  "upside": 0.0,
  "opinion": "투자의견 (예: 매수/적극매수 등, 텍스트)",
  "score": 0.0,
  "roe": 0.0,
  "debt": 0.0,
  "reason": "[섹터명] 순위 및 추천 사유 요약",
  "news_summary": "관련 뉴스/공시 요약 (문자열 또는 리스트)",
  "one_liner": "카드에 노출할 한 줄 요약",
  "disc_json": "관련 공시 리스트 (JSON 문자열)",
  "data_date": "YYYY-MM-DD"
}
```

* 섹터당 1~3건씩, 여러 섹터의 레코드를 하나의 배열에 함께 담는다 (과거처럼 value/momentum/dividend 파일을 나누지 않는다).
* `sector` 값은 Pool(`tr_stock_pool.sector`)에 실제 존재하는 섹터명과 정확히 일치해야 `/api/pool`에서 올바르게 매핑된다.
