# 내일의 공략주 — 추천종목 선정 작업 지침서

> **버전**: v1.2 | **최종 수정**: 2026-07-07  
> **담당**: 개발2팀 (김희선 파트장, 김도희 퀀트 자동화)  
> **적용 범위**: `cowork/` ↔ `trade/` 파이프라인 전체

---

## 1. 파이프라인 개요

```
[STEP 1] pool_collect.py  ─ 정량 스크리닝 → Pool DB 저장                  (Python 자동)
[STEP 2] select_top_10.py ─ 뉴스·공시 수집 → pool_context.json            (Python 자동)
[STEP 3] Claude 프롬프트   ─ 섹터별 1~10위 결정 → sector_rankings.json     (AI 수동)
[STEP 4] audit_save.py    ─ DB 적재 + sector_recommendations.json 생성    (Python 자동)
```

### 원칙

| 구분 | 설명 |
|------|------|
| DB | 단일 소스. Pool과 추천종목 데이터 모두 DB에 저장. |
| `pool_context.json` | AI 입력용 임시 파일. STEP 4 완료 후 삭제. |
| `sector_rankings.json` | AI 출력 결과. 재무 데이터 + 순위 + 이유 전체 포함. STEP 4 완료 후 삭제. |
| `sector_recommendations.json` | **운영서버 마이그레이션 payload**. STEP 4 생성, 영구 보존. STEP 5에서 운영서버 DB에 적재. |

> Claude가 `pool_context.json`을 읽으면서 재무 데이터를 이미 알고 있으므로,  
> `sector_rankings.json`에 모든 필드를 포함해 출력한다. 별도 병합 단계 없음.

---

## 2. 사전 조건 확인

- `trade/.env`에 DB 접속 정보가 올바르게 설정되어 있어야 한다.
- Python 실행 환경이 `c:\Pythons` 루트에서 동작해야 한다.

---

## 3. STEP 1 — Pool 구성

```bash
python cowork/pool_collect.py
```

- 정량 스크리닝: 대형주 4 섹터 × 10종목 + 상승주 4 섹터 × 10종목 + 기타 20종목
- `trade.db → tr_stock_pool` 테이블에 저장
- **JSON export 없음** (DB만 저장)

> 기존 Pool을 유지하고 순위만 재선정하는 경우 이 단계를 건너뛴다.

---

## 4. STEP 2 — 뉴스·공시 데이터 수집

```bash
python cowork/select_top_10.py
```

- DB `tr_stock_pool`에서 Pool 종목 조회
- 종목당 수집: 네이버 뉴스 최근 5건(제목+본문 200자) + DART 공시 최근 5건
- 결과를 `cowork/pool_context.json`으로 저장 (AI 입력용 임시 파일)

#### pool_context.json 구조

```json
{
  "반도체와반도체장비": [
    {
      "code": "005930",
      "name": "삼성전자",
      "current_price": 68000,
      "target_price": 93000,
      "upside": 36.8,
      "roe": 12.5,
      "debt": 29.9,
      "pbr": 1.2,
      "supply": "외인+/기관+",
      "news": [
        {"title": "...", "body": "...", "source": "연합뉴스", "date": "20260707"}
      ],
      "disclosures": [
        {"report_nm": "주요사항보고서(유상증자결정)", "rcept_dt": "20260706"}
      ]
    }
  ],
  "소프트웨어와서비스": [...]
}
```

---

## 5. STEP 3 — AI 섹터별 순위 결정 *(수동 작업)*

> Claude Code 터미널에서 아래 프롬프트를 그대로 실행한다.  
> **담당**: 개발2팀 김희선, 김도희

### 프롬프트

```
cowork/pool_context.json 파일을 읽고, 각 섹터별 10개 종목의 투자 우선순위를 결정해 주세요.

[평가 지침]
- 각 섹터 내 10개 종목을 서로 비교하여 상대적 우위를 1~10위로 결정합니다.
- 뉴스(제목+본문)와 공시를 통해 현재 시장 모멘텀과 이슈를 판단합니다.
- 상승여력(upside)과 수급(supply)은 보조 참고 지표로 활용합니다.
- 재무 지표(roe, debt, pbr)는 섹터 내 안정성 비교에 활용합니다.
- 호재(수주, 실적 개선, 신제품)가 있는 종목을 우선합니다.
- 악재(횡령, 손실, 상장폐지 리스크)가 있는 종목은 후순위로 내립니다.

[출력 형식]
결과를 cowork/sector_rankings.json 파일로 저장해 주세요.
pool_context.json의 재무 데이터를 그대로 포함하고, rank·reason·one_liner를 추가합니다.

{
  "섹터명": [
    {
      "rank": 1,
      "code": "종목코드",
      "name": "종목명",
      "sector": "섹터명",
      "current_price": 숫자,
      "target_price": 숫자,
      "upside": 숫자,
      "roe": 숫자,
      "debt": 숫자,
      "pbr": 숫자,
      "supply": "외인+/기관+",
      "data_date": "YYYY-MM-DD",
      "one_liner": "한 줄 투자 포인트",
      "reason": "선정 이유 (2~3문장, 뉴스·공시 근거 명시)",
      "news_summary": "pool_context.json of news list copied as string (else \"[]\")",
      "disc_json": "pool_context.json of disclosures list copied as string (else \"[]\")",
      "rec_type": "sector"
    },
    ...
  ],
  ...
}

섹터당 모든 10개 종목 포함, rank는 1~10 중복 없이 부여합니다.
```

### 품질 검토

| 확인 항목 | 기준 |
|-----------|------|
| 섹터 누락 여부 | pool_context.json과 동일한 섹터 수 |
| rank 정합성 | 섹터당 1~10 정수, 중복·누락 없음 |
| reason 충실도 | 뉴스·공시 기반 근거 포함 |
| 악재 종목 처리 | 명시적 악재 보도 종목 9~10위 배치 |

---

## 6. STEP 4 — 로컬 DB 적재 및 마이그레이션 payload 생성

```bash
python cowork/audit_save.py cowork/sector_rankings.json --type sector
```

### 역할

1. `sector_rankings.json` (섹터별 dict) → flat list 변환 후 `trade.db → tr_audit_recommendations` 적재
2. `trade/results/sector_recommendations.json` 생성 (운영서버 마이그레이션용 payload)
3. 임시 작업 파일 삭제 (수동): `cowork/pool_context.json`, `cowork/sector_rankings.json`

---

## 7. STEP 5 — 운영서버 반영 *(운영서버에서 직접 수행)*

1. 로컬에서 `sector_recommendations.json`을 GitHub에 push
2. 운영서버에서 `git pull`
3. 운영서버 UI의 **"추천 데이터 동기화"** 버튼 클릭 → `/api/targets/migrate` 호출 → 운영 DB 반영

---

## 8. 전체 체크리스트

```
[ ] STEP 1: pool_collect.py → tr_stock_pool 적재 확인
[ ] STEP 2: select_top_10.py → cowork/pool_context.json 생성 확인
[ ] STEP 3: Claude 프롬프트 → cowork/sector_rankings.json 생성 확인
[ ] STEP 3: 품질 검토 (섹터 수, rank 정합성, 전체 필드 포함 여부)
[ ] STEP 4: audit_save.py → tr_audit_recommendations 적재 확인
[ ] STEP 4: trade/results/sector_recommendations.json 생성 확인
[ ] STEP 4: 임시 파일 삭제 (pool_context.json, sector_rankings.json)
[ ] STEP 5: 운영서버 /api/targets/migrate 호출 (운영 배포 시)
[ ] UI: 섹터 탭 동적 표시 확인
[ ] UI: 종목 카드 내 뉴스·공시 섹션 표시 확인
```

---

## 9. 파일 역할 정리

| 파일 | 역할 | 처리 |
|------|------|------|
| `cowork/pool_context.json` | AI 입력용 임시 파일 | STEP 2 생성 → STEP 4 완료 후 삭제 |
| `cowork/sector_rankings.json` | AI 출력 결과 (재무+순위+이유 전체 포함) | STEP 3 생성 → STEP 4 완료 후 삭제 |
| `trade/results/sector_recommendations.json` | **백업용 결과물** (재무지표 + AI 순위 통합) | STEP 4 생성, 영구 보존 |
| `trade.db → tr_stock_pool` | Pool 원본 데이터 (단일 소스) | 영구 보존 |
| `trade.db → tr_audit_recommendations` | 추천종목 원본 데이터 (단일 소스) | 영구 보존 |
